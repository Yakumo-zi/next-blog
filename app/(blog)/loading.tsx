import { cn } from "@/utils";

const Loading = () => {
    return ( <div className={cn("container min-w-full min-h-screen flex justify-center items-center")}>
        <h1>Loading...</h1>
    </div> );
}
 
export default Loading;